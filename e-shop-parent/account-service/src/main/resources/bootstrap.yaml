spring:
  application:
    name: account-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR}
        namespace: dev
      config:
        server-addr: ${NACOS_ADDR}
        namespace: dev
        extension-configs:
          - dataId: datasource
            fileExtension: yaml
          - dataId: sentinel
            fileExtension: yaml
          - dataId: zipkin
            fileExtension: yaml
          - dataId: seata
            fileExtension: yaml