spring:
  application:
    name: product-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR}
        namespace: dev
      config:
        server-addr: ${NACOS_ADDR}
        namespace: dev
        extension-configs:
          - dataId: datasource
            fileExtension: yaml
          - dataId: sentinel
            fileExtension: yaml
#    sentinel: #Sentinel注册地址
#      transport:
#        dashboard: localhost:8080
#        port: 8719
